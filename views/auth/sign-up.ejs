<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/styles.css" />
  <title>Sign Up</title>
</head>
<body>
  <!-- Background Video -->
  <div class="video-background">
    <video autoplay muted loop playsinline class="background-video">
      <source src="/css/assets/mokaMeetBackground.mp4" type="video/mp4" />
      Your browser does not support the video tag.
    </video>
  </div>

  <!-- Sign Up Form -->
  <div class="signup-wrapper">
    <div class="signup-box">
      <p class="signup-title">Create a New Account</p>
      <form action="/auth/sign-up" method="POST">
        <div class="signup-group">
          <label class="signup-label" for="username">Username</label>
          <input placeholder="Enter a username" name="username" class="signup-input" type="text" required />
        </div>

        <div class="signup-group">
          <label class="signup-label" for="password">Password</label>
          <input placeholder="Enter a password" name="password" class="signup-input" type="password" required />
        </div>

        <div class="signup-group">
          <label class="signup-label" for="confirmPassword">Confirm Password</label>
          <input placeholder="Confirm your password" name="confirmPassword" class="signup-input" type="password" required />
        </div>

        <div class="signup-action">
          <button class="signup-btn" type="submit">Sign Up</button>
          <p>Already have an account? <a class="signup-link" href="/auth/sign-in">Sign in</a></p>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
